from setuptools import setup

setup(
    name='webserver',
    author='Mrigank Kumar',
    description='\n'.join((
        'Webserver modules, which are',
        '- mock_data.py',
        '- models.py'
    )),
    packages=['.']
)
